$("body").instantDefinitions();

$("body").instantDefinitions({
    underlineText: false,
    underlineColor: "#000000",
    underlineWeight: 0,
    backgroundColor: "#abfcab",
    backgroundOpacity: 0.4,
    highlightText: true,
    boldText: false,
    noStyle: false
  });

  $("body").instantDefinitions({
    maxSize: 350
  });
  $("body").instantDefinitions({
  showTitles: true
});